# build
build/
*.o
*.a
voice_assistant
asr_kws_process
tts_process
player/run
**/example/bin/

# 3rdparty/sherpa-onnx 预编译 SDK（按 README 下载；勿提交）
3rdparty/sherpa-onnx/*.tar.bz2
3rdparty/sherpa-onnx/*.tar.gz
3rdparty/sherpa-onnx/*.zip
3rdparty/sherpa-onnx/sherpa-onnx-v*/

# fetched / generated deps (see also 3rdparty/model/.gitignore)
.cursor

# 进程运行时日志（app_log_init）
data/

# 预合成 TTS 等二进制资源（Fork 请本地执行 tools/gen_mode_tts_wav.sh 生成）
assets/
